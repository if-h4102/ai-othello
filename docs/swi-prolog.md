# Configuration for Linux

## Installation

### Debian based

```shell
sudo apt-get install swi-prolog
```

### Arch

```shell
pacman -S swi-prolog
```

## Execution

```shell
swipl src/main.pl
```

## Exit SWI prolog

Hit `Ctrl` + `C` twice, then `e`.
